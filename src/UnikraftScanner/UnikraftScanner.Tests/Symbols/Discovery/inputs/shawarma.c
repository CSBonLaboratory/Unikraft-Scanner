#include <stdio.h>
#include <string.h>
// #include <zzz.h>
// #incldue "qwe.h"
/*

#ifdef WRONG
int z = 2;
#endif
*/
void f(){};
typedef char ifndef;
int main(){

       
#if defined(A)

    #ifndef ABCDEFG\
HIJK   LMN
    char a = '#'; ifndef CONFIG_LIB1;    
    
    f();
    f();

    f();
    #else
        #if defined(A) &&\
        \
        defined(B)\
        \
        && (defined(C) && defined(D))

            f(); f(); f();

            f();

            f();
        
        #elif defined(E)
            
/*EEEEEEEEEEEEEEEEEE*/
            f();
            #if defined(X) || defined(Y)

                f();
            #endif
            f();
            f();
            f();
            #if defined(X)
                f(); f();
            #elif defined(Y)
                f()\
                \
                ;

            #endif

            f();

            #ifndef X
                f(); ifndef\
                t = 'a';
            #endif

            f();

        #elif defined(F)
        #endif
    #endif
      
int xx = 8;
#else



int xxx = 9;
    // #if defined(A)
    //     int x = 8;
    // #endif
    // 
    // 

    /*
    #ifdef Q
        #if defined(Q)
        f();
        #endif
    #endif
    */

    #ifndef ABCDEFG\
\
\
\
HIJK


    f();
    #endif
#endif


}

